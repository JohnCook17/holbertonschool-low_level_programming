#include "holberton.h"
/**
 * add - adds two numbers
 *@a: a number
 *@b: a number
 *
 *Description: adds two numbers, does not check if they are numbers though
 *Return: returns the sum of two numbers.
 */
int add(int a, int b)
{
	return (a + b);
}
