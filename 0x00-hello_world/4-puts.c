/* prints text in str */
#include <stdio.h>
#include <string.h>
/**
 * main
 */
int main(void)
{
	char str[50];

	strcpy(str, "\"Programming is like building a multilingual puzzle");

	puts(str);
/**
 * returns 0 cause it does almost nothing
 */
	return (0);
}
